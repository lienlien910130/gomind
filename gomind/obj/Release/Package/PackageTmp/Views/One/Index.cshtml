@model IEnumerable<gomind.Models.One>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OneKind)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OneKind)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OneId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OneId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OneId })
        </td>
    </tr>
}

</table>
